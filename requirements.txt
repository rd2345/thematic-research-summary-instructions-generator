# Web interface dependencies
Flask==2.3.3
Werkzeug==2.3.7
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
blinker==1.6.2

# Core backend dependencies (required for both web and terminal)
boto3==1.34.144

# Terminal interface dependencies  
click==8.1.7

# Optional: Enhanced terminal features (uncomment if desired)
# tabulate==0.9.0
# tqdm==4.66.0